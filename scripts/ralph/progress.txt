# Ralph Progress Log
Started: 2026-02-10

## Codebase Patterns
(Patterns discovered during implementation will be added here)

---

## 2026-02-11 05:54:31 - PLAY-001
- Implemented complete project scaffolding with Vite + React 19 + TypeScript + Tailwind CSS v4
- Created package.json with all required dependencies:
  - React 19 and React DOM
  - CodeMirror 6 packages (@codemirror/state, view, lang-xml, language, commands, search, autocomplete)
  - Lucide React for icons
  - Tailwind CSS v4 alpha with Vite plugin
  - Vitest + Testing Library for testing
  - ESLint with TypeScript strict rules
- Set up tsconfig.json with strict mode and comprehensive type checking
- Created vite.config.ts with React and Tailwind plugins, including test configuration
- Created index.html with dark theme
- Implemented global styles with dark financial terminal theme using CSS custom properties
- Created basic App.tsx with centered "paymsg playground" heading
- Set up main.tsx entry point with StrictMode
- Configured ESLint to lint only src/ files (excluding config files)
- Created basic test setup with vitest.config in vite.config.ts
- Added App.test.tsx to verify rendering
- All quality checks passing:
  - ✅ npm run build
  - ✅ npm run lint
  - ✅ npm run test
  - ✅ npx tsc --noEmit

**Files changed:**
- package.json, package-lock.json
- tsconfig.json, tsconfig.node.json
- vite.config.ts
- eslint.config.js
- index.html
- src/main.tsx, src/App.tsx, src/App.test.tsx
- src/styles/globals.css
- src/test/setup.ts

**Learnings:**
- ESLint 9 with flat config requires careful file pattern matching - needed to exclude *.config.{js,ts} files from linting since they're not in the project tsconfig
- Tailwind CSS v4 uses @theme directive instead of traditional config file
- React 19 works well with strict TypeScript settings
- Using monospace font family for financial terminal aesthetic

---
## 2026-02-11 06:02:00 - PLAY-002
- Created scripts/copy-specs.sh to automate copying spec files from paymsg-specs sibling repo
- Successfully copied 18 JSON spec files into public/specs/ directory structure:
  - Reference data: currencies.json (170+ currencies), countries.json (240+ countries), iban_formats.json (80+ IBAN formats), bic_spec.json, swift_charsets.json (X/Y/Z character sets)
  - MT specs: mt103.json, mt202.json, mt940.json, mt942.json, block_structure.json
  - Mappings: mt103_pacs008.json, mt202_pacs009.json, mt940_camt053.json, mt942_camt052.json
  - Validation rules: pacs008_rules.json, pacs009_rules.json, camt052_rules.json, camt053_rules.json
- Implemented src/specs/loader.ts with comprehensive TypeScript interfaces and typed loader functions:
  - Reference data types: Currency, Country, IbanFormat, BicSpec, SwiftCharsets
  - MT spec types: MtSpec, MtField, MtBlockSpec with mandatory/optional status
  - Mapping types: MappingSpec, FieldMapping with transform types (direct, split, merge, lookup, derived, conditional)
  - Validation rule types: RulesSpec, ValidationRule with severity levels
  - Loader functions: loadCurrencies(), loadCountries(), loadIbanFormats(), loadBicSpec(), loadSwiftCharsets(), loadMtSpec(type), loadBlockStructure(), loadMapping(pair), loadRules(type)
  - Helper functions: getCurrency(), getCountry(), getIbanFormat(), isValidCurrency(), isValidCountry()
- Created comprehensive test suite with 23 unit tests covering all loader functions
- Set up test environment with fetch mock using Node.js file system for reading spec files in test environment
- Added @types/node to dev dependencies for test environment
- All quality checks passing: build, lint, test, tsc

**Files changed:**
- scripts/copy-specs.sh (new)
- public/specs/ (18 new JSON files in subdirectories)
- src/specs/loader.ts, src/specs/loader.test.ts (new)
- src/test/setup.ts (added fetch mock for testing)
- tsconfig.json (added node types)
- package.json, package-lock.json (@types/node)

**Learnings:**
- Fetch API in Node.js test environment requires mocking with file system reads
- TypeScript strict mode catches potential issues with Response.json() return type - needed explicit type assertion
- ESLint no-base-to-string rule requires careful handling of URL/Request objects in string context
- The block_structure.json uses "description" field instead of "version" at root level
- Organizing specs by category (reference, mt-specs, mappings, rules) makes the codebase more maintainable
- Using a generic loadJson<T> helper with type parameter ensures type safety across all loaders

---
## 2026-02-11 06:05:00 - PLAY-003
- Created src/engine/types.ts with comprehensive TypeScript type definitions:
  - MessageType enum with all 8 supported message types (MT103, MT202, MT940, MT942, pacs.008, pacs.009, camt.053, camt.052)
  - MessageFormat enum (MT, MX, UNKNOWN)
  - Severity enum (ERROR, WARNING, INFO)
  - MtMessage structure with 5 blocks (block1-5)
  - MtField interface with tag, value, and optional subfields
  - MxMessage structure with namespace, document, and root element
  - ValidationResult and ValidationIssue interfaces
  - DataLossWarning and DataLossCategory for translation warnings
  - TranslationResult interface
  - DetectionResult interface
  - Type guards: isMtMessage() and isMxMessage()
- Implemented src/engine/detector.ts with message format and type detection:
  - detectMessage(): auto-detects format (MT/MX) and specific message type
  - MT detection: looks for '{1:' prefix
  - MX detection: looks for '<?xml' or '<Document' prefix
  - detectMtType(): extracts message type from Block 2 (e.g., {2:I103...})
  - detectMxType(): extracts message type from XML namespace (e.g., pacs.008.001.10)
  - Helper functions: isMtFormat(), isMxFormat(), getMessageTypeName()
  - Returns confidence score (1.0 for identified types, 0.7 for format-only, 0 for unknown)
- Created comprehensive test suite with 29 tests covering:
  - All 4 MT message types (MT103, MT202, MT940, MT942)
  - All 4 MX message types (pacs.008, pacs.009, camt.053, camt.052)
  - Both Input and Output MT formats
  - Edge cases: empty messages, whitespace, invalid format, partial messages, unknown types
  - Helper functions: isMtFormat(), isMxFormat(), getMessageTypeName()
- All quality checks passing: build, lint, test (53 tests), tsc

**Files changed:**
- src/engine/types.ts (new)
- src/engine/detector.ts (new)
- src/engine/detector.test.ts (new)
- scripts/ralph/prd.json (PLAY-003 marked as passes: true)

**Learnings:**
- TypeScript type guards (isMtMessage, isMxMessage) provide compile-time safety when working with union types
- Confidence scores are useful for UI feedback when detection is ambiguous
- MT Block 2 format differs for Input (I) vs Output (O) but both contain the 3-digit message type
- XML namespace detection via regex is reliable even for partial documents
- Messages starting with '<?xml' should be detected as MX format even if incomplete (reasonable UX)

---
## 2026-02-11 06:15:00 - PLAY-004
- Implemented complete MT message parser in src/engine/parser.ts
- Implemented parseMtMessage() with 5-block structure parsing:
  - Block 1: Basic header (app_id, service_id, LT address, session, sequence)
  - Block 2: Application header with Input/Output format support
  - Block 3: User header with tag-value pairs
  - Block 4: Text block with field extraction and multi-line value support
  - Block 5: Trailer with tag-value pairs
- Implemented compound field parsers for:
  - Field 32A: Date, Currency, Amount (YYMMDDCCCAA,AA)
  - Field 33B: Currency, Amount
  - Fields 50K, 59: Account + Name/Address (multi-line)
  - Fields 52A, 56A, 57A, 58A: Account + BIC
  - Fields 60F, 62F, 64: D/C indicator + Date + Currency + Amount (balance fields)
  - Field 61: Statement line with complex subfield parsing (value_date, entry_date, debit_credit, funds_code, amount, type_code, reference, supplementary, additional_info)
- Handled both \r\n and \n line endings
- Created comprehensive test suite with 36 tests covering:
  - MT103: All blocks, all compound fields, multi-line values
  - MT202: Interbank transfer format
  - MT940: Statement with multiple field 61 entries, balance fields
  - MT942: Minimal interim report
  - Edge cases: missing blocks, different line endings, Output format, fields without account prefix
- All quality checks passing: build, lint, test (89 tests total), tsc

**Files changed:**
- src/engine/parser.ts (new, 370 lines)
- src/engine/parser.test.ts (new, 458 lines)
- scripts/ralph/prd.json (PLAY-004 marked as passes: true)

**Learnings:**
- MT field 61 (statement line) has complex optional components: entry date can be 1, 2, or 4 digits, or absent
- TypeScript strict mode requires careful handling of regex match results and array indexing - all captures must be checked for undefined
- Using bracket notation (obj['key']) instead of dot notation (obj.key) is required for Record<string, string> types in strict mode
- Block 3 and Block 5 use nested braces format {TAG:VALUE} which requires non-capturing groups in regex: (?:\{[^}]+\})+
- Multi-line field values are common in MT messages - parser must accumulate lines until next field tag
- Entry date in field 61 is detected by checking if characters after value date are all digits vs D/C indicator
- The parseMxMessage function is a placeholder for PLAY-005 - marked with underscore prefix to avoid unused parameter lint error

---
## 2026-02-11 06:20:45 - PLAY-005
- Implemented complete MX (XML) message parser in src/engine/parser.ts
- Implemented parseMxMessage() using browser's native DOMParser:
  - Parses XML and detects parsing errors
  - Extracts namespace from root element
  - Returns MxMessage structure with document, root element, and namespace
- Implemented XML navigation helpers:
  - getElementByPath(): Navigate by path string (e.g., "FIToFICstmrCdtTrf/GrpHdr/MsgId") and return text content
  - getElementNodeByPath(): Return Element node itself for accessing attributes
  - getElementsByPath(): Get all matching elements for repeated elements (e.g., multiple balance entries)
- Parsing support for all 4 MX message types:
  - pacs.008 (FIToFICstmrCdtTrf): Customer credit transfer with GrpHdr, CdtTrfTxInf, party details
  - pacs.009 (FICdtTrf): FI credit transfer with simplified structure
  - camt.053 (BkToCstmrStmt): Bank statement with account info and multiple balance entries
  - camt.052 (BkToCstmrAcctRpt): Interim report structure
- Created comprehensive test suite with 58 tests:
  - Parsing tests for all 4 MX types using realistic sample data from paymsg-specs
  - Element extraction tests for GrpHdr, transaction details, party info, accounts
  - Attribute access tests (e.g., Ccy attribute on amount elements)
  - Multiple element tests (e.g., Balance entries in camt.053)
  - Error handling tests for malformed XML
  - Edge case tests (empty content, non-existent paths)
- All quality checks passing: build, lint, test (111 tests total), tsc
- Updated parseMessage() to dispatch to parseMxMessage() instead of throwing error

**Files changed:**
- src/engine/parser.ts (+141 lines)
- src/engine/parser.test.ts (+357 lines)
- scripts/ralph/prd.json (PLAY-005 marked as passes: true)

**Learnings:**
- DOMParser is browser-native and doesn't require external dependencies - perfect for client-side XML parsing
- TypeScript HTMLCollection requires explicit type casting: `current.children[i] as Element` to avoid implicit any
- ESLint @typescript-eslint/no-unnecessary-condition rule catches redundant null checks after type assertions
- documentElement is never null in a parsed Document, so null check is unnecessary
- XML namespace detection via element.namespaceURI works reliably for ISO 20022 messages
- Path-based element navigation is more intuitive than querySelector with namespace prefixes
- Using localName instead of tagName ignores namespace prefixes and simplifies matching
- getElementsByPath pattern is useful for repeated elements like statement entries or balances
- Testing with realistic sample data from sibling repo ensures parser works with actual message structures
- All 4 MX message types share GrpHdr pattern but have different root elements and transaction structures

---
## 2026-02-11 06:25:00 - PLAY-006
- Implemented complete message formatting module in src/engine/formatter.ts
- Created formatMt() function for MT message formatting:
  - Parses 5-block structure with proper indentation
  - Formats Block 3 and Block 5 nested tags on separate lines
  - Formats Block 4 fields with 2-space indentation and proper alignment
  - Handles multi-line field values correctly
  - Normalizes line endings (CRLF → LF)
- Created formatXml() function for XML message formatting:
  - Proper indentation with configurable spacing (default: 2 spaces)
  - Line breaks between elements for readability
  - Preserves attributes on the same line as tags
  - Handles self-closing tags, XML declarations, and comments
  - Detects opening/closing tags and adjusts indentation level accordingly
- Created formatJson() function as a wrapper around JSON.stringify with configurable indentation
- Implemented 43 comprehensive tests covering:
  - MT103, MT202, MT940 formatting with all block types
  - XML formatting for ISO 20022 message structures (pacs.008, camt.053)
  - Custom indentation levels (2-space, 4-space, 1-space)
  - Edge cases: empty strings, whitespace-only, CRLF line endings, malformed blocks
  - Content preservation tests to ensure formatting doesn't alter message data
  - Multi-line field values and nested structures
- All quality checks passing: build, lint, test (154 tests total), tsc

**Files changed:**
- src/engine/formatter.ts (new, 267 lines)
- src/engine/formatter.test.ts (new, 468 lines)
- scripts/ralph/prd.json (PLAY-006 marked as passes: true)

**Learnings:**
- TypeScript strict mode requires explicit null/undefined checks for regex match captures - use `if (!content) return block;` pattern
- ESLint @typescript-eslint/restrict-plus-operands requires string type guards before concatenation - use `if (!char) continue;` to satisfy type checker
- ESLint no-regex-spaces rule prefers `{4}` over counting spaces in regex patterns
- Returning empty string for whitespace-only input is better UX than returning the input unchanged
- MT block parsing requires tracking brace depth to handle nested braces in Block 3 and Block 5
- XML formatting requires careful tracking of tag types (opening, closing, self-closing) to manage indentation levels
- Using `const` instead of `let` for variables that don't change helps catch logic errors and improves code clarity
- The `matchAll()` method returns an iterator of regex capture groups - always check if captured groups exist before using them
- Testing both CRLF and LF line endings ensures cross-platform compatibility
- Formatters should preserve exact content while only improving presentation - no data transformation

---
## 2026-02-11 06:32:00 - PLAY-007
- Implemented complete sample messages collection in src/samples/messages.ts
- Created 9 realistic sample messages sourced from paymsg-specs testdata:
  - 5 MT messages: MT103 SEPA EUR, MT103 USD wire with intermediary, MT202 interbank, MT940 daily statement (6 entries), MT942 intraday report
  - 4 MX messages: pacs.008 SEPA credit, pacs.009 FI transfer, camt.053 daily statement (3 entries), camt.052 interim report (4 entries)
- Each sample includes: id, label, description, format, messageType, and full message content
- All samples use realistic financial data with proper IBANs, BICs, amounts, and references
- Created comprehensive test suite (26 tests) verifying:
  - All samples parse without errors
  - Correct format and type detection
  - Parsed structure validation for key fields
  - Message content quality (amounts, IBANs, BICs, currencies)
- All quality checks passing: build, lint, test (180 tests total), tsc

**Files changed:**
- src/samples/messages.ts (new, 1044 lines total for both files)
- src/samples/messages.test.ts (new)
- scripts/ralph/prd.json (PLAY-007 marked as passes: true)

**Learnings:**
- TypeScript strict mode requires bracket notation for index signatures: use `obj?.['field']` instead of `obj?.field` when accessing Record<string, any> subfields
- Adding proper type guards (`if ('block1' in parsed && parsed.block4)`) prevents TypeScript undefined access errors
- Sample messages should be comprehensive enough to test all parser features (multi-line fields, compound fields, multiple entries)
- Using realistic data from the sibling paymsg-specs repo ensures samples align with actual message standards
- Each MT message type has distinct characteristics: MT103 has parties and intermediaries, MT202 is simpler for FI-to-FI, MT940/MT942 focus on statement entries
- MX messages have more verbose XML structure but clearer semantic naming than MT messages
- Good sample descriptions help users understand the use case and message characteristics at a glance
- Test coverage should verify both parsing success and content quality (e.g., proper decimal separators, valid BICs/IBANs)

---
## 2026-02-11 06:38:00 - PLAY-008
- Implemented complete CodeMirror 6 editor component with syntax highlighting
- Created src/components/mtLang.ts with custom MT syntax highlighting extension:
  - StreamLanguage-based parser for SWIFT MT message structure
  - Color-coded syntax: block delimiters (yellow), field tags (cyan), amounts (green), currencies (orange), dates (purple), BICs (blue)
  - Pattern matching for MT-specific elements: :TAG:, {BLOCK:}, amounts with comma decimals, currency codes, dates, BICs
- Implemented src/components/Editor.tsx with full-featured CodeMirror integration:
  - Support for both 'mt' and 'xml' language modes with automatic syntax switching
  - Dark theme using @codemirror/theme-one-dark
  - Basic editor features: line numbers, bracket matching, history, search, autocomplete
  - Controlled component with value/onChange props
  - Read-only mode support for output display
  - Placeholder support for empty editors
  - Line wrapping enabled for long messages
  - Responsive container that fills parent element
- Added comprehensive CSS styling in globals.css:
  - Editor container styles for proper layout
  - MT syntax highlighting color classes matching the terminal theme
  - Monospace font and proper line height for code readability
- Created comprehensive test suite with 25 tests covering:
  - Component rendering with various props
  - Language mode switching (MT/XML)
  - Read-only vs editable modes
  - Value updates and controlled component behavior
  - MT syntax highlighting with realistic message samples
  - XML syntax highlighting with ISO 20022 messages
  - Edge cases: long content, special characters, unicode, unmounting
  - Integration scenarios with complete MT103 and pacs.008 messages
- Installed @codemirror/theme-one-dark package for dark theme support
- All quality checks passing: build, lint, test (205 tests total), tsc

**Files changed:**
- package.json, package-lock.json (@codemirror/theme-one-dark added)
- src/components/mtLang.ts (new, 87 lines)
- src/components/Editor.tsx (new, 155 lines)
- src/components/Editor.test.tsx (new, 214 lines)
- src/styles/globals.css (+45 lines for editor and MT syntax styling)
- scripts/ralph/prd.json (PLAY-008 marked as passes: true)

**Learnings:**
- CodeMirror 6 uses a modular architecture - no single 'codemirror' package, must import from individual @codemirror/* packages
- StreamLanguage.define() is the easiest way to create custom syntax highlighting for non-standard formats like MT messages
- Basic setup must be manually assembled from individual extensions (lineNumbers, history, bracketMatching, etc.)
- React integration requires two useEffect hooks: one for editor creation/recreation, one for controlled value updates
- Must use useCallback for onChange handler to prevent unnecessary editor recreation
- The editor should NOT be recreated when value changes - only when language, readOnly, or placeholder changes
- ESLint react-hooks/exhaustive-deps can be safely disabled when we intentionally exclude onChange from dependencies (it's memoized with useCallback)
- Using ViewUpdate type from @codemirror/view provides proper type safety for update listeners
- EditorView.lineWrapping is important for financial messages which can have long field values
- CSS custom properties from the terminal theme integrate well with CodeMirror's oneDark theme
- Testing CodeMirror components requires checking for presence of .cm-editor and .cm-content DOM elements
- The component correctly handles language switching without memory leaks (old editor is destroyed before creating new one)

---
## 2026-02-11 06:45:30 - PLAY-009
- Implemented complete application layout with header, toolbar, split panes, and status bar
- Created src/components/Header.tsx with app title and subtitle
- Created src/components/Toolbar.tsx with action buttons:
  - Parse, Validate, Translate, Format buttons with keyboard shortcut hints
  - Sample message dropdown selector
  - Clear button to reset application state
  - Color-coded buttons: blue (Parse), green (Validate), purple (Translate), amber (Format)
- Created src/components/StatusBar.tsx to display detected message format and type
  - Shows format (SWIFT MT / ISO 20022 MX / No message)
  - Shows message type with descriptions (MT103, pacs.008, etc.)
  - Handles all 8 message types plus UNKNOWN
- Implemented split-pane layout in App.tsx using CSS Grid:
  - Two-column grid layout with equal columns
  - Input editor on left with "Input" label
  - Output editor on right with "Output" label
  - Both editors use CodeMirror with auto-detected syntax highlighting
- Integrated format auto-detection:
  - Detects format and type when sample is loaded
  - Automatically switches editor language mode (MT vs XML)
  - Updates status bar in real-time
- Added placeholder handlers for Parse, Validate, Translate, Format actions (to be implemented in later stories)
- Full responsive layout using Tailwind classes with dark theme
- Created comprehensive test suites:
  - Header.test.tsx (3 tests)
  - Toolbar.test.tsx (11 tests)
  - StatusBar.test.tsx (8 tests)
  - App.test.tsx (8 tests) - tests layout, sample loading, button interactions
- All quality checks passing: build, lint, test (234 tests), tsc

**Files changed:**
- src/components/Header.tsx, Header.test.tsx (new)
- src/components/Toolbar.tsx, Toolbar.test.tsx (new)
- src/components/StatusBar.tsx, StatusBar.test.tsx (new)
- src/App.tsx (complete rewrite with full layout)
- src/App.test.tsx (updated for new layout)
- src/samples/messages.ts (added `samples` export alias)
- scripts/ralph/prd.json (PLAY-009 marked as passes: true)

**Learnings:**
- CSS Grid with `grid-cols-2` creates a perfect split-pane layout for editors
- TypeScript's `exactOptionalPropertyTypes: true` requires explicit `| undefined` in interface props, not just `?:`
- CodeMirror content is not directly accessible via `getByText` in tests - must check for editor existence via `.cm-editor` class
- Sample message selector needs proper type annotations to avoid `any` type errors in strict mode
- StatusBar must handle all MessageType enum values including UNKNOWN to satisfy Record<MessageType, string>
- Lucide React icons integrate seamlessly with Tailwind and provide consistent sizing
- Using CSS Grid's `overflow-hidden` on parent with `flex-1` on children ensures editors fill available space
- Toolbar buttons with tooltips showing keyboard shortcuts improve UX even before shortcuts are implemented
- Border colors (border-zinc-800) and background colors (bg-zinc-900, bg-zinc-950) create good visual hierarchy
- Auto-detecting message format on input change provides instant feedback to users
- Component composition (Header + Toolbar + Content + StatusBar) creates clean, maintainable layout structure

---
